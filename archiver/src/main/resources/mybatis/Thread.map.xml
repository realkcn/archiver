<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="org.kbs.archiver.persistence.ThreadMapper">
    <select id="countAll" resultType="int">
        select count(*)  from thread;
    </select>
    
    <select id="selectAll" resultType="org.kbs.archiver.ThreadEntity">
        select * from thread order by threadid asc
    </select>
    
    <insert id="insert" parameterType="org.kbs.archiver.ThreadEntity">
        insert into thread(threadid,boardid,subject,posttime,articlenumber,author,lastreply,lastposttime,
          orginid,encodingurl) values (#{threadid},#{boardid},#{subject},#{posttime},#{articlenumber},#{author},
          #{lastreply},#{lastposttime},#{orginid},#{encodingurl})
         ON DUPLICATE KEY UPDATE boardid=#{boardid},subject=#{subject},posttime=#{posttime},articlenumber=#{articlenumber},
          author=#{author},lastreply=#{lastreply},lastposttime=#{lastposttime},orginid=#{orginid},encodingurl=#{encodingurl} 
    </insert>
    
    <update id="update" parameterType="org.kbs.archiver.ThreadEntity">
        update thread set boardid=#{boardid},subject=#{subject},posttime=#{posttime},articlenumber=#{articlenumber},
          author=#{author},lastreply=#{lastreply},lastposttime=#{lastposttime},orginid=#{orginid},encodingurl=#{encodingurl} 
          where threadid=#{threadid}
    </update>

    <delete id="delete" parameterType="long">
        delete from thread where threadid=#{threadid}
    </delete>

    <select id="get" parameterType="long" resultType="org.kbs.archiver.ThreadEntity">
        select * from thread where threadid=#{threadid}
    </select>
    <select id="getByOriginId" parameterType="long" resultType="org.kbs.archiver.ThreadEntity">
        select * from thread where originid=#{originid}
    </select>
    <select id="getByEncodingUrl" parameterType="String" resultType="org.kbs.archiver.ThreadEntity">
        select * from thread where encodingurl=#{encodingurl}
    </select>
    <select id="getThreadsOnBoard" parameterType="long" resultType="org.kbs.archiver.ThreadEntity">
        select * from thread where boardid=#{boardid}
    </select>
    <cache eviction="FIFO" flushInterval="3600000" size="1024" readOnly="true" />
</mapper>